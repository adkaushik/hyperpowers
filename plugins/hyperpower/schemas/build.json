{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/adkaushik/hyperpowers/schemas/build.json",
  "title": "Build contract",
  "description": "Output of the Build stage and of every fix-loop round's builder. What changed, the test that failed first, the commands that ran, and the real-path check. A contract that fails this schema is a hard stop, not a warning. The next stage never runs on it.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "step",
    "status",
    "changed_files",
    "tests",
    "gates_run",
    "real_path_check",
    "assumptions",
    "notes"
  ],
  "properties": {
    "step": {
      "type": "string",
      "const": "build"
    },
    "run_id": {
      "type": [
        "string",
        "null"
      ],
      "description": "The run folder under .hyperpower/runs/. null when the stage ran without a run journal."
    },
    "prompt_hash": {
      "type": [
        "string",
        "null"
      ],
      "description": "sha256 of the prompt that produced this contract. The orchestrator stamps it, so it is absent on the contract a stage hands to hp-validate. /hyperpower:resume reads it."
    },
    "cache_key": {
      "type": [
        "object",
        "null"
      ],
      "description": "Stamped by hp-journal after validation. hp-journal owns this shape, so the schema records that the field is legal and checks nothing inside it."
    },
    "recorded_at": {
      "type": "string",
      "description": "Stamped by hp-journal when it writes the step file, so it is absent on the contract a stage hands to hp-validate."
    },
    "files_read": {
      "type": "array",
      "description": "Every path this stage read. /hyperpower:resume re-hashes this list to detect drift. A file read and left out is drift the resume check cannot see.",
      "items": {
        "type": "string"
      }
    },
    "assumptions": {
      "type": "array",
      "description": "Every fact this stage relied on that it did not read in front of it. Present even when empty: an absent array reads to /hyperpower:why as zero assumptions declared. Carry an upstream assumption forward with its original id, claim and declared_at.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "claim",
          "source",
          "confidence",
          "declared_at",
          "checkable",
          "status"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "Unique within the run, not within the step. a1, a2."
          },
          "claim": {
            "type": "string",
            "description": "The fact relied on, in one sentence."
          },
          "source": {
            "type": "string",
            "enum": [
              "rulebook",
              "inferred",
              "asked"
            ]
          },
          "confidence": {
            "type": "string",
            "enum": [
              "high",
              "medium",
              "low"
            ]
          },
          "declared_at": {
            "type": "string",
            "description": "The step that first declared it.",
            "enum": [
              "route",
              "understand",
              "plan",
              "design",
              "build",
              "gates",
              "review",
              "fix",
              "render"
            ]
          },
          "checkable": {
            "type": [
              "string",
              "null"
            ],
            "description": "The file path or command that settles the claim later. null when nothing settles it, which stamps the status unresolved."
          },
          "status": {
            "type": "string",
            "description": "declared until the gate-time pass re-reads checkable and stamps it.",
            "enum": [
              "declared",
              "verified",
              "refuted",
              "unresolved"
            ]
          }
        }
      }
    },
    "rejected": {
      "type": "array",
      "description": "Options considered and dropped. /hyperpower:why --rejected reads this array by name, so it is flat and top level.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "option",
          "reason"
        ],
        "properties": {
          "option": {
            "type": "string",
            "description": "The option dropped."
          },
          "reason": {
            "type": "string",
            "description": "Why it lost."
          }
        }
      }
    },
    "notes": {
      "type": "array",
      "description": "Anything the next stage needs. Never shaped and never capped.",
      "items": {
        "type": "string"
      }
    },
    "status": {
      "type": "string",
      "description": "rulebook_conflict when the needed change would violate a rule. The human or the fix loop decides.",
      "enum": [
        "done",
        "blocked",
        "rulebook_conflict"
      ]
    },
    "changed_files": {
      "type": "array",
      "description": "Every file edited or created.",
      "items": {
        "type": "string"
      }
    },
    "tests": {
      "type": [
        "object",
        "null"
      ],
      "description": "null only when the builder stopped before writing a test.",
      "additionalProperties": false,
      "required": [
        "added",
        "command",
        "first_run",
        "result"
      ],
      "properties": {
        "added": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "command": {
          "type": [
            "string",
            "null"
          ],
          "description": "commands.test_scoped, with {files} substituted."
        },
        "first_run": {
          "type": "string",
          "description": "A test that has never failed proves nothing. The only recordable value is failed. A test that passed on its first run does not test the change: fix the test, do not record a pass here.",
          "enum": [
            "failed"
          ]
        },
        "result": {
          "type": "string",
          "description": "The result after the change.",
          "enum": [
            "pass",
            "fail"
          ]
        }
      }
    },
    "gates_run": {
      "type": "array",
      "description": "Every command the builder ran, recorded verbatim.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "command",
          "exit_code",
          "output_tail"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "command": {
            "type": "string"
          },
          "exit_code": {
            "type": [
              "integer",
              "null"
            ]
          },
          "output_tail": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      }
    },
    "real_path_check": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "attempted",
        "method",
        "result",
        "blocked_reason"
      ],
      "properties": {
        "attempted": {
          "type": "boolean",
          "description": "Never true for a check that did not run. A green suite is not evidence a user-facing change works."
        },
        "method": {
          "type": [
            "string",
            "null"
          ]
        },
        "result": {
          "type": [
            "string",
            "null"
          ]
        },
        "blocked_reason": {
          "type": [
            "string",
            "null"
          ],
          "description": "The exact reason the environment blocked it."
        }
      }
    }
  }
}
