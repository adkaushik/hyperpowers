{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/adkaushik/hyperpowers/schemas/route.json",
  "title": "Route contract",
  "description": "Output of the Route stage. Picks one task class, the ordered stage list, and the model tier for every stage in it. A contract that fails this schema is a hard stop, not a warning. The next stage never runs on it.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "step",
    "run_id",
    "prompt_hash",
    "files_read",
    "requirement",
    "task_class",
    "why",
    "stages",
    "skipped",
    "tiers",
    "models",
    "waits_for_human",
    "candidates",
    "ratcheted_from",
    "assumptions",
    "rejected",
    "notes"
  ],
  "properties": {
    "step": {
      "type": "string",
      "const": "route"
    },
    "run_id": {
      "type": [
        "string",
        "null"
      ],
      "description": "The run folder under .hyperpower/runs/. null when the stage ran without a run journal."
    },
    "prompt_hash": {
      "type": [
        "string",
        "null"
      ],
      "description": "sha256 of the prompt that produced this contract. The orchestrator stamps it, so it is absent on the contract a stage hands to hp-validate. /hyperpower:resume reads it."
    },
    "cache_key": {
      "type": [
        "object",
        "null"
      ],
      "description": "Stamped by hp-journal after validation. hp-journal owns this shape, so the schema records that the field is legal and checks nothing inside it."
    },
    "recorded_at": {
      "type": "string",
      "description": "Stamped by hp-journal when it writes the step file, so it is absent on the contract a stage hands to hp-validate."
    },
    "files_read": {
      "type": "array",
      "description": "Every path this stage read. /hyperpower:resume re-hashes this list to detect drift. A file read and left out is drift the resume check cannot see.",
      "items": {
        "type": "string"
      }
    },
    "assumptions": {
      "type": "array",
      "description": "Every fact this stage relied on that it did not read in front of it. Present even when empty: an absent array reads to /hyperpower:why as zero assumptions declared. Carry an upstream assumption forward with its original id, claim and declared_at.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "claim",
          "source",
          "confidence",
          "declared_at",
          "checkable",
          "status"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "Unique within the run, not within the step. a1, a2."
          },
          "claim": {
            "type": "string",
            "description": "The fact relied on, in one sentence."
          },
          "source": {
            "type": "string",
            "enum": [
              "rulebook",
              "inferred",
              "asked"
            ]
          },
          "confidence": {
            "type": "string",
            "enum": [
              "high",
              "medium",
              "low"
            ]
          },
          "declared_at": {
            "type": "string",
            "description": "The step that first declared it.",
            "enum": [
              "route",
              "understand",
              "plan",
              "design",
              "build",
              "gates",
              "review",
              "fix",
              "render"
            ]
          },
          "checkable": {
            "type": [
              "string",
              "null"
            ],
            "description": "The file path or command that settles the claim later. null when nothing settles it, which stamps the status unresolved."
          },
          "status": {
            "type": "string",
            "description": "declared until the gate-time pass re-reads checkable and stamps it.",
            "enum": [
              "declared",
              "verified",
              "refuted",
              "unresolved"
            ]
          }
        }
      }
    },
    "rejected": {
      "type": "array",
      "description": "Options considered and dropped. /hyperpower:why --rejected reads this array by name, so it is flat and top level.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "option",
          "reason"
        ],
        "properties": {
          "option": {
            "type": "string",
            "description": "The option dropped."
          },
          "reason": {
            "type": "string",
            "description": "Why it lost."
          }
        }
      }
    },
    "notes": {
      "type": "array",
      "description": "Anything the next stage needs. Never shaped and never capped.",
      "items": {
        "type": "string"
      }
    },
    "requirement": {
      "type": "string",
      "description": "The requirement as given, unedited."
    },
    "task_class": {
      "type": "string",
      "description": "The one chosen class. The names come from task-classes.yml.",
      "enum": [
        "triage",
        "trivial",
        "one-file-fix",
        "dependency",
        "bug",
        "refactor",
        "feature",
        "ui-feature"
      ]
    },
    "why": {
      "type": "string",
      "description": "One sentence naming the signal in the requirement, not the class definition."
    },
    "stages": {
      "type": "array",
      "description": "The class's stages list, verbatim and in order. route is never a member.",
      "items": {
        "type": "string",
        "enum": [
          "understand",
          "plan",
          "design",
          "build",
          "gates",
          "review",
          "fix",
          "render"
        ]
      }
    },
    "skipped": {
      "type": "array",
      "description": "Every stage in the pipeline this class does not run, as bare stage names. skills/route writes the names, so the schema records names.",
      "items": {
        "type": "string",
        "enum": [
          "understand",
          "plan",
          "design",
          "build",
          "gates",
          "review",
          "fix",
          "render"
        ]
      }
    },
    "tiers": {
      "type": "object",
      "description": "One entry per stage in stages. A stage in stages with no entry here stops the run.",
      "additionalProperties": false,
      "required": [],
      "properties": {
        "understand": {
          "type": "string",
          "enum": [
            "judgment",
            "mechanical"
          ]
        },
        "plan": {
          "type": "string",
          "enum": [
            "judgment",
            "mechanical"
          ]
        },
        "design": {
          "type": "string",
          "enum": [
            "judgment",
            "mechanical"
          ]
        },
        "build": {
          "type": "string",
          "enum": [
            "judgment",
            "mechanical"
          ]
        },
        "gates": {
          "type": "string",
          "enum": [
            "judgment",
            "mechanical"
          ]
        },
        "review": {
          "type": "string",
          "enum": [
            "judgment",
            "mechanical"
          ]
        },
        "fix": {
          "type": "string",
          "enum": [
            "judgment",
            "mechanical"
          ]
        },
        "render": {
          "type": "string",
          "enum": [
            "judgment",
            "mechanical"
          ]
        }
      }
    },
    "models": {
      "type": "object",
      "description": "The two resolved model ids, so the journal records what actually ran. There is no third tier.",
      "additionalProperties": false,
      "required": [
        "judgment",
        "mechanical"
      ],
      "properties": {
        "judgment": {
          "type": "string"
        },
        "mechanical": {
          "type": "string"
        }
      }
    },
    "review_slices_max": {
      "type": [
        "integer",
        "null"
      ],
      "description": "The class's slice ceiling. Optional: task-classes.yml sets it to null for a class that skips Review, and skills/route omits the field entirely in that case. Present as an integer whenever Review is in stages."
    },
    "waits_for_human": {
      "type": "boolean",
      "description": "True only when Design runs."
    },
    "candidates": {
      "type": "array",
      "description": "Every class that fit, lightest first.",
      "items": {
        "type": "string",
        "enum": [
          "triage",
          "trivial",
          "one-file-fix",
          "dependency",
          "bug",
          "refactor",
          "feature",
          "ui-feature"
        ]
      }
    },
    "ratcheted_from": {
      "type": [
        "string",
        "null"
      ],
      "description": "The lighter class the ratchet passed over, or null.",
      "enum": [
        "triage",
        "trivial",
        "one-file-fix",
        "dependency",
        "bug",
        "refactor",
        "feature",
        "ui-feature",
        null
      ]
    }
  }
}
