# agents

Agent definitions. One file per agent role. An agent earns a file when it has a job no existing agent does — not when a name would be nice.

Empty for now. See [docs/architecture.md](../../../docs/architecture.md).
