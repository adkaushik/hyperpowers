# hooks

Session hooks. Session start injects the protocol. The optional commit gate blocks a bare git commit until it goes through the harness.

Empty for now. See [docs/architecture.md](../../../docs/architecture.md).
